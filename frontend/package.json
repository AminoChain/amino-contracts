{
  "name": "Amino_NFT_Minter_UI",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "resolutions": {
    "react-error-overlay": "6.0.9"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "start-legacy": "react-scripts start",
    "build-and-deploy": "react-scripts build && mv ../admin-page-build/.git/ ../ && rm -rf ../admin-page-build/* && mv build/* ../admin-page-build && cd .. && mv .git/ admin-page-build/ && cd admin-page-build && git add -A && git commit -m \"Update\" && git push origin master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@web3-react/core": "^6.1.9",
    "@web3-react/injected-connector": "^6.0.7",
    "eslint": "^7.11.0",
    "ethers": "^5.5.4",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "swr": "^1.2.1",
    "web3": "^1.7.3"
  },
  "devDependencies": {
    "@types/node": "17.0.15",
    "@types/react": "17.0.39",
    "eslint-config-react": "^1.1.7",
    "react-error-overlay": "^6.0.9",
    "react-scripts": "4.0.3",
    "typescript": "4.5.5"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
